Large scale testing for clusters. 

Note: This is not included in travis tests.

To do:
-Need to put alternatives to mpiexec into sim_funcs scripts (eg. srun/aprun)
-Also need to allow for diff no. procs per node and num nodes per worker.
-Investigate with aprun.
